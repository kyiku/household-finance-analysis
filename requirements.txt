# アプリ実行 + テストに必要なパッケージ。
# バージョンは動作確認済みの値に固定している(再現性のため)。
# 完全な依存ツリーの記録は requirements-lock.txt を参照。
# ノートブックを動かす場合は requirements-notebooks.txt も追加でインストールする。

# --- データ処理・可視化 ---
pandas==3.0.3
matplotlib==3.11.0
japanize-matplotlib==1.1.3    # matplotlibの日本語フォント設定
setuptools<81                 # japanize-matplotlib が distutils を使うため上限が必要

# --- 機械学習 ---
scikit-learn==1.9.0

# --- アプリ ---
streamlit==1.61.1

# --- テスト ---
pytest==9.1.1
pytest-cov==7.1.0
