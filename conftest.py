"""pytest がリポジトリ直下を `sys.path` に追加するためのマーカーファイル。

中身は空で構わないが、**このファイルを削除すると `tests/` から `from src...` を
import できなくなり、全テストモジュールがコレクションエラーになる**。
pytest は最上位の conftest.py があるディレクトリを import ルートとして扱うため、
`src/` `ui/` をトップレベルパッケージとして解決するのにこのファイルが必要。
"""
